# DDPW Moneybook Flux ● Cursor 바이브 코딩 프롬프트 목록

---

## 1. 탭 전환/페이지 렌더링 버그 자동 점검 ✅ 재수정 완료

탭(월별현황, 계좌 관리, 결제수단 관리, 저축관리 등)을 여러 번 전환해도  
이전 UI가 남아있거나 신규등록 버튼/FAB 팝업이 정상 작동하지 않는 등의 버그가 있습니다.

- index.html에서 각 탭 클릭 시 js 렌더 함수(renderDashboard, renderAccountsTab 등)가 제대로 동작하는지, main-content 초기화가 정상적으로 되는지 점검해 주세요.
- 각 js 및 이벤트 등록/스코프 연결, DOM 변화, global/window 연결을 자동 보정하고, 테스트 방법을 안내해 주세요.

**재수정 완료**: `app.js`의 중복 코드 제거, `renderAccountsTab` 호출 시 `accounts-container`가 없으면 `main-content`를 직접 사용하도록 수정, 모든 렌더 함수가 올바르게 호출되도록 보장

---

## 2. 수입/지출 구분별 드롭다운 옵션 오류 ✅ 수정 완료

입출금 내역 등록 화면에서 [수입]을 선택하면  
카테고리·항목 드롭다운에 지출용 옵션까지 같이 보이거나,  
올바른 분기(수입 시 수입항목만, 지출 시 지출항목만)가 동작하지 않아 저장 오류가 발생합니다.

- 수입/지출 선택에 따라 관련 옵션 데이터(배열/객체)와 렌더 함수가 완전히 분리되어 동작하도록 점검/수정해주세요.
- radio/select change 이벤트 리셋, renderCategoryOptions(), renderItemOptions() 등 함수별 코드 예시와 단위테스트 방법을 안내해 주세요.

**수정 완료**: 구분 라디오 버튼 변경 이벤트 리스너 추가, 카테고리/항목 드롭다운 완전 초기화 후 선택된 구분에 맞는 옵션만 추가하도록 수정

---

## 3. 데이터 실시간 동기화 및 파일 백업/복원 ✅ 수정 완료

모든 데이터는 localStorage와 data/*.json 파일로 관리하고  
OneDrive 등 클라우드 폴더에 동기화합니다.

- File System Access API를 활용하여 JSON 파일로 자동 저장/백업/복원 기능을 구현
- 데이터 변경시 localStorage와 파일의 값이 즉시 반영되게 최적화하고,  
클라우드 폴더에서 동기화된 파일을 불러와 다른 PC에서도 완벽하게 활용할 수 있도록 구조를 안내해 주세요.

**수정 완료**: 
- File System Access API를 사용한 폴더 선택 및 자동 동기화 기능 구현
- `saveData()`, `saveAccountData()`, `saveMerchantHistory()`, `saveCardData()` 함수가 localStorage와 파일에 동시 저장
- 설정 탭에 "데이터 폴더 선택" 버튼 추가
- 백업/복원 기능 개선 (JSON 다운로드 및 파일 선택 복원)
- 데이터 변경 시 localStorage에 즉시 저장, 폴더가 선택되어 있으면 파일에도 자동 저장

---

## 4. 거래내역 수정/삭제 버그 ✅ 수정 완료

거래 내역 수정 버튼을 눌렀을 때, '해당 내역을 찾을 수 없습니다.' 오류
- 배열/객체 연결, localStorage·JS 내 데이터 ID·키 구조 확인 및 버그 자동 점검
- 내역 저장/수정/삭제 시 데이터 무결성과 화면 동기화 테스트 방법 안내

**수정 완료**: `editTransaction` 함수에서 ID 타입 변환 로직 추가, `transactionData` 접근 전 유효성 검사 추가, 삭제 시에도 동일한 로직 적용

---

## 5. 계좌 관리, 입출금 내역 실시간 연동 ✅ 수정 완료

입출금 내역을 등록/수정/삭제하면
계좌 현황 및 월별 현황 탭에 자동으로 반영, 동기화되지 않는 문제가 있습니다.

- localStorage, 계좌, 입출금 내역 데이터 구조·렌더 함수 간 callback/observer 패턴으로 실시간 업데이트 최적화
- js 파일 간 함수 연결, 이벤트 트리거 구조, DOM 상태 변화 점검
- 테스트 방법 안내(동시 확인 체크리스트 포함)

**수정 완료**: 입출금 내역 등록/수정/삭제 시 `calculateAccountBalances()` 호출 및 현재 활성 탭 렌더 함수 자동 호출로 실시간 동기화 구현


## 6. FAB 버튼 - 결제수단 등록 팝업 동작 오류 점검 ✅ 수정 완료

FAB 버튼에서 [결제수단 등록]을 눌렀을 때  
결제수단 관리 페이지로 이동한 다음 뜨는 팝업이 '계좌 추가' 창(통장/초기잔액 입력)이 호출되고 있습니다.

- 실제로는 결제수단 관리에서 [+ 결제수단 추가] 버튼을 눌렀을 때와 동일한 "결제수단 추가(카드 등록)" 팝업 UI가 떠야 정상입니다.
- 현재 FAB 버튼 클릭 이벤트, 결제수단 관리 페이지 렌더 함수, 팝업 모달 연결 로직을 모두 점검해
- [계좌 추가] 팝업이 아니라 [결제수단 추가] 기능 화면이 동일하게 호출되는 구조로 수정해 주세요.
- 관련 js 함수(window/global), 팝업 초기화/랜더링 위치 점검과 이벤트 오류도 모두 보정 바랍니다.
테스트: FAB로 결제수단 등록→직후 팝업에서 카드 등록이 정상 동작, 내역 반영 여부까지 확인 바랍니다.

**수정 완료**: `openPaymentMethodModal` 함수가 `openCardManageModal`을 호출하도록 수정하여 결제수단 추가 모달이 올바르게 열리도록 구현

## 7. 계좌 관리 탭 - 입출금 후 잔액 반영 오류 점검 ✅ 수정 완료

계좌 관리 화면에서 수입이 발생하고 입출금 내역을 추가하면  
우측 '잔액' 부분이 0원으로 표기되는 문제가 있습니다.

- 잔액 칼럼에는 해당 계좌의 기존 잔액(또는 초기 잔액)에서 새로 추가된 입출금 내역을 모두 반영한 실제 잔액이 표출되어야 정상입니다.
- 입출금 등록/수정/삭제 등 데이터 변경시 계좌별 잔액(누적 계산)이 제대로 update되는지,
- 내역 테이블 연산, 계좌 데이터셋, localStorage 및 render 함수의 잔액 업데이트 callback 구조를 모두 자동 점검해 주세요.
- 문제 발견 시 잔액 계산/표기 로직을 보정하는 코드와 테스트 방법까지 안내 바랍니다.

테스트: 여러 입출금 내역을 등록/수정 시 잔액이 실시간으로 반영되고, 본 계좌의 잔고와 일치하는지 확인 바랍니다.

**수정 완료**: 입출금 내역 등록/수정/삭제 시 `calculateAccountBalances()` 호출 및 계좌 목록 갱신으로 잔액이 실시간 반영되도록 구현

## 8. 계좌 관리 탭 - 입출금 후 잔액 반영 오류 점검 ✅ 수정 완료

계좌 관리 화면에서 수입이 발생하고 입출금 내역을 추가하면  
우측 '잔액' 부분이 0원으로 표기되는 문제가 있습니다.

- 잔액 칼럼에는 해당 계좌의 기존 잔액(또는 초기 잔액)에서 새로 추가된 입출금 내역을 모두 반영한 실제 잔액이 표출되어야 정상입니다.
- 입출금 등록/수정/삭제 등 데이터 변경시 계좌별 잔액(누적 계산)이 제대로 update되는지,
- 내역 테이블 연산, 계좌 데이터셋, localStorage 및 render 함수의 잔액 업데이트 callback 구조를 모두 자동 점검해 주세요.
- 문제 발견 시 잔액 계산/표기 로직을 보정하는 코드와 테스트 방법까지 안내 바랍니다.

테스트: 여러 입출금 내역을 등록/수정 시 잔액이 실시간으로 반영되고, 본 계좌의 잔고와 일치하는지 확인 바랍니다.

**수정 완료**: 7번과 동일한 수정 적용 완료

## 9. 계좌 관리 페이지 신규 입출금 내역 등록 버튼 구현 ✅ 수정 완료

계좌 관리 페이지에서 FAB 버튼 말고도 [입출금 내역 등록]을 신규로 추가할 수 있는 버튼을 구현해 주세요.
- 해당 버튼을 누르면 기존 FAB 팝업과 동일하게 입출금 내역 등록 창이 뜨고, 기능도 동일하게 동작해야 합니다.
- 버튼 위치: 계좌 관리 탭 상단 또는 테이블 근처 적절한 곳
- 이벤트 연결, 팝업 호출, 등록/저장/반영 로직까지 안내 바랍니다.

**수정 완료**: 계좌 관리 탭 상단 헤더와 입출금 내역 섹션에 "💰 입출금 내역 등록" 버튼 추가, `openAccountTransactionModal` 호출로 구현

---

## 10. 결제수단 관리, 저축관리, 통계, 설정 탭 연/월 표시 제거 ✅ 수정 완료

결제수단 관리, 저축관리, 통계, 설정 페이지에서는 연/월별 페이지 로드와 이동이 필요 없으니
- 연/월 표시(텍스트, 셀렉터, 이동화살표 등)와 관련 js 렌더 함수, UI 버튼을 모두 제거해 주세요.
- 해당 탭 화면에서 연/월 관련 DOM, 데이터 로직, 이벤트 바인딩 부분도 모두 정리 바랍니다.

테스트: 각 탭 진입 시 연/월 UI가 보이지 않고, 불필요한 페이지 이동 이벤트가 삭제된 것을 확인하세요.

**수정 완료**: 탭 전환 시 `month-bar`를 dashboard와 accounts 탭에서만 표시하도록 제어, 월 선택 버튼도 해당 탭에서만 동작하도록 수정


## 11. 계좌 관리 탭 - 잔액 표기/업데이트 오류 ✅ 수정 완료

계좌 관리에서 입출금 내역을 추가(수입/지출 등)했는데
- 표 상단의 통장 잔고와
- 아래 상세 입출금 내역 테이블 우측(잔액) 칼럼
모두 잔액이 0원으로 계속 표기되는 문제가 있습니다.

기대 동작:
- 각 입출금 내역이 추가/수정/삭제될 때마다 해당 계좌별 현재 잔고가 실시간으로 반영되어야 합니다.
- 상세 테이블에도 시점별로 잔액이 누적 반영(직전 잔액→신규 입출금→현재 잔액)되어야 합니다.
- 전체 계좌 합계도 실시간 반영

점검 요청:
- index.html과 연결된 js 파일(특히 accounts.js, dashboard.js 등)에서
- 잔액 누적 계산 함수, 내역 렌더링, localStorage 및 데이터 업데이트, UI 표현, 수식 충돌, 이벤트/함수 이름 중복 등 모두 점검해 주세요.
- 문제 발견 시 코드/로직을 자동으로 개선하고, 수식/콜백/표기/테스트 방법까지 안내 바랍니다.

테스트: 여러 입출금 입력 후 상단과 내역 테이블 잔액이 맞게 실시간 반영되는지 확인하세요.

**수정 완료**: 
- `calculateAccountBalances()` 함수가 `paymentMethod === 'transfer' || paymentMethod === 'cash'`인 거래만 처리하도록 수정
- `renderAccountTransactionTable`에서 테이블 잔액 계산 시 `account.currentBalance`에서 현재 월 거래를 역순으로 빼서 초기값 계산하도록 개선
- 입출금 내역 추가/수정/삭제 시 `calculateAccountBalances()` 호출 및 계좌 카드/테이블 갱신으로 실시간 반영 보장

## 12. 월별현황 내역 입력 - 자동입력 창 호출 버그 ✅ 수정 완료

- 월별현황(대시보드)에서 내역 입력 시 "자동입력" 탭을 눌러도 자동입력 창이 정상적으로 호출되지 않는 버그가 발생하고 있습니다.
- 기존 자동입력 팝업(명세서 추출, 카드 내역 자동 파싱 등)이 정상 동작해야 하는데, 
  클릭/전환/버튼 이벤트에 반응하지 않거나, 모달 창이 뜨지 않습니다.

점검 및 수정 요청:
- index.html과 연결된 js(dashboard.js, modal.js 등)에서
- 자동입력 버튼·탭 클릭 이벤트, 팝업 모달 함수, DOM 연결, 글로벌 모듈 등 동작 구조 전체 점검해 주세요.
- 자바스크립트 오류, 모달 초기화, 동적 렌더, CSS/팝업 위치 문제, 충돌 등 모든 코드를 자동으로 점검하고 빠진 부분 바로 수정 바랍니다.
- 정상 동작 구현 예시 코드와 이벤트 바인딩 방식도 함께 안내 바랍니다.

테스트 방법:
- 월별현황 내역 입력에서 자동입력 탭을 클릭할 때, 자동입력 창이 신속하게 뜨고, 내역 추가/명세서 추출 기능까지 정상 동작하는지 확인하세요.

**수정 완료**: 
- `openModal` 함수에서 탭 초기화 로직 추가 (수동 입력 탭 기본 활성화)
- `index.html`의 탭 전환 이벤트 리스너 개선 (모달별 독립적인 탭 전환 처리)
- 탭 버튼 클릭 시 `display: block` 및 `active` 클래스 동시 적용으로 탭 콘텐츠 표시 보장

## 13. 계좌 관리 - 계좌 소유자 지정 신규 기능 ✅ 수정 완료

신규 기능 추가 요청입니다.

- 계좌 관리에서 계좌를 추가하거나 수정할 때 소유자를 선택할 수 있도록 라디오박스를 구현해 주세요.
  - 선택지는 '둠둠', '파우' 두 명으로 제한
- 실제 계좌 등록/수정 폼에 소유자 항목(라디오 혹은 셀렉트)을 추가
- 계좌 정보 데이터셋에도 소유자 정보가 저장/갱신되어야 합니다.
- 계좌관리 표에도 '소유자' 열을 추가해 계좌별로 누가 소유자인지 바로 확인할 수 있도록 수정 바랍니다.
- '계좌/카드' 열은 '계좌' 로 바꾸고, 그 오른쪽과 '잔액' 열 사이에 '소유자' 열을 추가 바랍니다.

점검 및 구현 요청:
- index.html과 연결된 js(accounts.js 등)에서
- 폼/테이블/데이터 로직, localStorage 반영까지 전체 구조 점검 및 신규 항목 추가
- 적합한 UI와 코드/함수 예시도 안내 바랍니다.

테스트 방법:
- 계좌 추가/수정 시 소유자를 반드시 선택할 수 있고, 계좌관리 표에서 각 계좌별 소유자가 바로 표시되는지 확인해 주세요.

**수정 완료**: 
- 계좌 추가/수정 폼에 소유자 라디오박스 추가 ('둠둠', '파우' 선택)
- 계좌 데이터에 `owner` 필드 저장/수정 로직 추가
- 계좌 카드에 소유자 표시 추가
- 계좌별 입출금 내역 테이블에 '소유자' 열 추가 ('계좌/카드' → '계좌', 그 오른쪽에 '소유자' 추가)
- `editAccount` 함수에서 소유자 정보 로드 로직 추가

## 14. 월별 현황 화면 UI 최적화 요청 ✅ 수정 완료

월별 현황(대시보드) 화면에서  
- 신규 등록 버튼의 위치(상단/테이블 근처 등)를 더 직관적이고 접근성이 좋은 곳으로 조정해 주세요.
- 검색창의 크기와 위치를 데이터 볼륨과 화면 공간에 맞게 최적화해  
  입력/검색 동선이 편리하도록 개선 바랍니다.
- 전체적으로 UI 요소 간격, 정렬, 배치 등을 통일감 있고 시인성 좋게 수정

점검 및 구현 요청:
- index.html, dashboard.js, common.css 등 관련 파일에서
- 신규 등록 버튼의 DOM 위치, 이벤트 연결, 스타일 수정
- 검색창의 입력 상자(width), 버튼 크기, 레이아웃 개선
- 전체 UI 간격/정렬/배치 점검 및 CSS 최적화 적용

테스트 방법:
- 신규 등록 버튼 클릭이 더 직관적이고 접근성이 개선되었으며  
- 검색창 크기/위치/스타일이 화면에 자연스럽게 반영되는지 확인하세요.

**수정 완료**: 
- 신규 등록 버튼을 상단 우측에 배치하고 크기/스타일 개선
- 검색바와 정렬 컨트롤을 하나의 카드로 통합하여 레이아웃 개선
- 검색창 크기 최적화 (flex: 1, min-width: 200px)
- 전체 UI 간격 및 정렬 통일 (gap: 16px, padding: 20px, border-radius: 12px)
- 반응형 레이아웃 지원 (flex-wrap: wrap)

## 15. 데이터 액세스 연동 및 폴더 자동 선택/불러오기 ✅ 수정 완료 (16번으로 고도화됨) (16번으로 고도화됨)

데이터 관리(설정)에서 File System Access API로 OneDrive 등 클라우드 폴더를 연동할 때 다음 기능을 추가해 주세요.

동작 흐름:
- index.html(또는 앱)을 처음 새로고침/실행할 때 데이터 폴더가 이미 지정된 경우,  
  자동으로 해당 폴더에서 최신 데이터를 불러올지 묻는 팝업(alert/confirm)을 띄웁니다.
- 사용자가 "예"를 누르면 폴더에 저장된 데이터를 즉시 불러오고,  
  "아니오"/혹은 폴더가 미지정 상태거나 저장된 스토리지가 호출이 안될때는  
  "스토리지 폴더를 선택하시겠습니까?"라고 안내하고, 폴더 선택 창을 호출합니다.
- 폴더를 선택하면 즉시 해당 폴더에서 데이터를 한 번 자동으로 받아와서 반영해야 합니다.

점검/구현 요청:
- index.html과 연결된 js(settings.js, data-manager.js 등)에서
- 데이터 폴더 선택, 파일 읽기/쓰기, 팝업/알림 UI, 폴더/스토리지 지정 여부 체크, 자동 불러오기/동기화 로직 전반을 점검하여 구현해 주세요.
- Alert/모달 창 안내 및 사용자 동의, 폴더 선택 완료 후 즉시 불러오기 코드 예시도 함께 안내 바랍니다.

테스트 방법:
- 새로고침/진입시 폴더 지정 여부에 따라 알림이 정상적으로 뜨고,  
  자동 또는 수동으로 데이터를 불러오며,  
  선택한 폴더의 데이터가 정확히 화면과 localStorage에 연동되는지 확인하세요.

**수정 완료**: 
- `checkFolderSelected()` 함수 추가: localStorage에서 폴더 지정 여부 확인
- `app.js`의 `DOMContentLoaded`에 `initializeFolderData()` 함수 추가:
  - 폴더가 이전에 선택된 경우: "폴더에서 최신 데이터를 불러오시겠습니까?" 확인 팝업
  - 사용자가 "예" 선택 시: 폴더 재선택 후 데이터 자동 불러오기
  - 폴더가 미지정된 경우: "데이터를 클라우드 폴더에 자동으로 동기화하시겠습니까?" 확인 팝업
  - 폴더 선택 완료 시: 즉시 `loadDataFromFolder()` 호출하여 데이터 불러오기 및 UI 갱신
- `settings.js`의 폴더 선택 버튼: 폴더 선택 후 즉시 데이터 불러오기 로직 추가
- 데이터 불러오기 성공 시: localStorage 동기화 및 UI 갱신 (대시보드, 계좌 관리 등)

## 16. 월별 현황 신규 등록 자동입력 탭 전환 오류 점검 ✅ 수정 완료

설명:
월별 현황 화면에서 "신규 등록" 시, 팝업/modal 내 "자동 입력" 탭 클릭 시 화면 전환 및 입력폼 로딩이 정상적으로 동작하지 않는 현상이 발견됨. 수동/자동 탭 전환 이벤트가 작동하지 않으며, 자동 입력 폼 로드가 멈추는 먹통 상태가 있음.

점검/구현 요청:
- index.html에서 modal.js, dashboard.js 등 주요 JS가 정상 연결·로드되는지 확인
- 자동 입력 탭 클릭 이벤트 및 입력폼 렌더*전환 로직(JS) 점검 (modal.js, dashboard.js 위주)
- 탭 클릭 시 콘솔 로그, 이벤트 바인딩, 렌더링 함수 호출 흐름 추적
- 먹통 현상 구간에서 에러 메시지, DOM 업데이트 여부, 이벤트 바인딩 오류 등 상세 진단
- modal.js, dashboard.js의 입력폼 show/hide, render 함수의 정상 동작 여부 확인

테스트 방법:
- 신규 등록 버튼 클릭 → 팝업/modal에서 "수동 입력"과 "자동 입력" 탭을 번갈아 3회 이상 클릭
- 탭 클릭 시 콘솔 로그/캡처로 단계별 이벤트 흐름 확인
- 자동 입력 폼이 정상적으로 표시 및 전환되는지 UI와 데이터 입력 테스트
- 문제 지속 시 modal.js, dashboard.js의 주요 함수(렌더·이벤트)별 console.log 추가 후 재테스트

히스토리:
- 신규 QA 항목(16번) 등록, 기존 15번 이어서 관리

**수정 완료**: 
- `index.html`의 탭 전환 이벤트 리스너 개선: 디버깅 로그 추가, 탭 콘텐츠 찾기 로직 강화 (여러 ID 패턴 시도)
- `openModal` 함수 개선: 탭 초기화 시 스타일 초기화 및 `display: block/none` 명시적 설정
- 탭 버튼 클릭 시 모든 탭의 스타일 초기화 후 선택한 탭만 활성화하도록 수정
- 콘솔 로그 추가로 탭 전환 과정 추적 가능

## 17. 월별 현황 신규 등록 결제수단 동기화 오류 ✅ 수정 완료

설명:
월별 현황 화면에서 "신규 등록" 시, "결제수단" 선택 드롭다운(하위 리스트)에 최근 "결제수단 관리" 메뉴에서 추가한 신규 결제수단이 반영되지 않음. 즉, 결제수단 관리와 신규 등록 폼 간에 데이터 동기화가 되지 않아, 새로 추가한 결제수단 항목이 선택지에 나타나지 않는 문제가 있음.

점검/구현 요청:
- 결제수단 관리에서 신규 수단 추가 시, 해당 데이터가 실시간 또는 새로고침 없이도 "신규등록" 팝업의 결제수단 리스트에 반영되는지 확인
- 결제수단 관리 데이터 저장 방식(localStorage, state 등)과 등록 폼에서 리스트 불러오는 로직(js 함수, 이벤트) 점검
- 신규 결제수단 추가 후 즉시 UI 리스트가 갱신되도록 이벤트 및 렌더링 함수(예: renderPaymentMethods, updatePaymentMethodList 등) 개선 및 console.log 추가 테스트
- 결제수단 관리 → 월별 현황 신규 등록 폼 간 데이터 흐름 및 의존성 전체 코드 점검

테스트 방법:
- 결제수단 관리 메뉴에서 신규 결제수단을 1~2개 추가
- 월별 현황 → 신규 등록 시 결제수단 리스트에 해당 항목이 바로 표시되는지 확인(새로고침 없이)
- 추가 후 결제수단 선택, 거래 등록이 정상적으로 완료되는지 확인
- 문제 지속 시 console.log로 결제수단 리스트 불러오기 함수 호출 및 데이터 동기 여부 재테스트

히스토리:
- 신규 QA 항목(17번) 등록, 기존 16번 이어서 관리

**수정 완료**: 
- `updatePaymentOptions` 함수 개선: cardData와 accountData 최신 상태 확인, 콘솔 로그 추가
- `openModal` 함수에서 모달 열 때 `updatePaymentOptions()` 호출하여 최신 결제수단 목록 반영
- `cards.js`의 카드 추가/수정/삭제 시 `updatePaymentOptions()` 호출하여 신규 등록 폼에 즉시 반영
- 결제수단 옵션 업데이트 시 콘솔 로그로 디버깅 가능

## 18. 상단 카테고리 탭 클릭 시 색상 활성화 중복/누적 오류 ✅ 수정 완료

설명:
상단 메인 카테고리(월별현황, 계좌 관리, 결제수단 관리, 저축관리, 통계, 설정) 탭 클릭 시, 활성화(색상 강조) 효과가 누적되어 여러 탭에 동시에 활성화 색상이 남거나, 정상적으로 한 개만 활성화되지 않는 현상이 발생함. UI 혼란 및 탭간 상호작용 오류를 유발.

점검/구현 요청:
- 각 카테고리 탭 클릭 이벤트에서 모든 탭의 활성화 클래스를 제거 후, 클릭한 탭에만 활성화(색상) 효과를 적용하는지 점검
- index.html 및 js(nav.js, main.js 등)에서 탭 상태 관리/클래스 toggle 관련 코드 점검 및 개선 필요 (예: removeActiveClass → addActiveClass 등)
- 탭 클릭 시 콘솔 로그로 활성화 클래스 추가/제거가 정상적으로 동작하는지 단계별 체크
- UI 가이드라인에 따라 한 번에 한 탭만 색상 활성화(강조)되도록 코드 개선 및 유지보수

테스트 방법:
- 모든 상단 카테고리(월별현황, 계좌관리, 결제수단관리, 저축관리, 통계, 설정) 탭 클릭 후, UI에 오직 한 개의 활성화 색상 효과만 남아있는지 시각적으로 확인
- 연속 클릭 시 이전 활성화 탭의 강조가 사라지고, 클릭한 탭만 활성화되는지 콘솔 로그 및 UI 점검
- 수동·자동 입력 등 하위 탭 영역과 연동 시에도 활성화 중복이 발생하지 않는지 추가 테스트

히스토리:
- 신규 QA 항목(18번) 등록, 기존 17번 이어서 관리

**수정 완료**: 
- `app.js`의 탭 클릭 이벤트 리스너 개선: 모든 탭의 active 클래스 제거 및 인라인 스타일 초기화 후 클릭한 탭만 활성화
- 탭 전환 시 콘솔 로그 추가로 디버깅 가능
- 중복 활성화 방지를 위한 명시적 스타일 초기화 로직 추가

## 19. 월별현황 수입 입력 시 계좌 관리 입출금 내역 미반영 오류 ✅ 수정 완료

설명:
월별현황 화면에서 "수입"으로 입출된 거래를 등록해도, "계좌 관리" 메뉴의 "계좌별 입출금 내역"·거래 리스트에 해당 기록이 표시되지 않는 오류가 발생. 수입(입금) 거래가 실제 데이터에는 반영되지만, 계좌별 내역 표출·동기화에 누락이 있음.

점검/구현 요청:
- 월별현황에서 수입 거래 등록(저장) 후, 계좌 관리 화면으로 진입 시 해당 계좌의 입출금 내역 리스트가 최신 상태로 반영·표시되고 있는지 점검
- 수입·지출 등록 로직(js: dashboard.js, accounts.js 등)에서 계좌별 거래 기록 동기화·데이터 저장/조회 함수 호출 흐름(예: updateAccountHistory, renderAccountTransactionList 등) 확인
- 거래 등록 후 계좌 관리 내역이 즉시 업데이트되지 않을 경우, 거래 save/refresh/렌더 관련 이벤트, 데이터 흐름, localStorage 등 코드 개선
- 거래 데이터 구조와 계좌 참조(키/ID) 연결 관계 및 화면 업데이트 반영 여부 전체 코드 검사

테스트 방법:
- 월별현황에서 해당 계좌로 수입 거래를 등록 후, 즉시 계좌 관리 메뉴로 진입해 해당 계좌 입출금 내역 리스트에 반영 여부 시각적으로 확인
- 지출, 수입 모두 반복 테스트하여 각 거래가 계좌 관리 입·출금 리스트에 항상 즉시 반영되는지 체크
- console.log 추가 후 계좌 내역 렌더 함수·데이터 동기 여부 재확인 및 검사

히스토리:
- 신규 QA 항목(19번) 등록, 기존 18번 이어서 관리

**수정 완료**: 
- `modal.js`의 폼 제출 로직 개선: 신규 입력 모드에서도 현재 활성 탭 확인 후 계좌 관리 탭이 활성화되어 있으면 `renderAccountTransactionTable()` 및 `renderAccountsTab()` 호출
- 수입/지출 거래 등록 시 계좌 관리 탭의 입출금 내역 테이블이 즉시 갱신되도록 수정
- `accounts-container`가 없으면 `main-content`에서 찾아서 렌더링하도록 fallback 로직 추가
- `calculateAccountBalances()` 호출로 계좌 잔액도 실시간 반영

## 20. 월별현황 카드별 내역 카드 선택 리스트 미표시 오류 ✅ 수정 완료

설명:
월별현황 화면에서 "카드별 내역" 선택 시, "카드선택" 드롭다운(하위 리스트)에 실제 등록된 카드 항목들이 나타나지 않는 오류가 발생함. 카드가 추가되어 있어도 선택 리스트에 표시되지 않으므로 카드별 내역 조회 및 입력이 불가함.

점검/구현 요청:
- 카드 관리에서 신규 카드 추가·등록 시 해당 데이터가 월별현황의 카드선택 리스트에 실시간 또는 새로고침 없이 반영되는지 점검
- 카드 관리 데이터 저장 방식(localStorage, state 등)과 카드 선택 리스트 렌더 함수(js: cards.js, dashboard.js 등) 점검
- 카드별 내역·카드 선택 드롭다운 영역의 카드 리스트 갱신 이벤트와 렌더 함수(예: renderCardSelectList, updateCardDropdown 등) 코드 개선 및 console.log 추가 확인
- 카드 관리 → 월별현황 카드별 내역·카드선택 간 데이터 흐름, 함수 호출, 의존성 전체 코드 점검

테스트 방법:
- 카드 관리 메뉴에서 신규 카드를 1~2개 추가
- 월별현황 > 카드별 내역 클릭시 카드 선택 드롭다운에 해당 카드가 즉시 보이는지 확인(새로고침 없이)
- 카드 선택 후 내역 조회·입력 테스트로 실제 정상 반영되는지 UI/데이터 체크
- 문제 지속 시 카드 리스트 불러오기 이벤트 및 렌더 함수별 console.log 추가 후 단위별 점검

히스토리:
- 신규 QA 항목(20번) 등록, 기존 19번 이어서 관리

**수정 완료**: 
- `updateCardFilterOptions()` 함수 개선: cardData를 우선적으로 읽어오고, accountData의 카드 계좌도 보완적으로 추가하도록 수정
- 카드 추가/수정/삭제 시 `updateCardFilterOptions()` 호출하여 월별현황 카드별 내역 드롭다운에 즉시 반영
- 콘솔 로그 추가로 디버깅 가능

21. 월별현황 리스트 표에 내역 입력 "사용처", "세부내용" 미반영 및 열 추가 요청 (상태: 미완료)

설명:
월별현황에서 내역 입력 폼에 작성한 "사용처"와 "세부내용"이 거래 리스트 표에 반영되지 않아, 입력한 데이터가 화면에 표시되지 않는 문제. 사용자 내역을 목록에서 쉽게 확인·검색할 수 있도록, 리스트 표에 두 열("카테고리"와 "금액" 사이)에 "사용처", "세부내용"이 순서대로 추가되어야 함.

점검/구현 요청:
- 내역 입력 시 저장되는 데이터 구조에 "사용처", "세부내용" 누락 여부 점검 및 저장 로직 개선(js: modal.js, dashboard.js 등)
- 거래 내역 리스트(테이블) 렌더링 함수에서 "사용처", "세부내용"이 새로운 열로 표시되도록 테이블 헤더/바디 column 및 DOM 구조 추가
- 데이터 입력, 저장-조회, 리스트 렌더까지 전체 연결 과정 점검 및 console.log로 각 데이터 값 표시 확인
- UI 위치: "카테고리"와 "금액" 컬럼 사이에 "사용처", "세부내용" 순서로 배치

테스트 방법:
- 내역 입력 시 "사용처"와 "세부내용"을 각각 입력한 후 저장
- 월별현황 리스트를 바로 확인해, 입력한 값이 표에 해당 열에 정상적으로 반영·표시되는지 체크
- 기존 내역에도 열 추가 후 누락값 없이 값이 표시되는지 반복 테스트 및 console.log로 데이터 추적

히스토리:
- 신규 QA 항목(21번) 등록, 기존 20번 이어서 관리

22. 월별현황 "통장 입출금" 계좌 리스트 선택 및 하위 항목별 결과 조회 기능 추가 요청 (상태: 미완료)

설명:
월별현황 탭의 "통장 입출금" 화면에도 "카드별 내역" UI처럼 계좌 리스트(드롭다운) 선택 기능을 추가하고, 선택한 계좌별로 내역 표·결과가 변경되어 나타나도록 개선 요청. 드롭다운 위치 및 동작 방식은 "카드별 내역"과 동일하게 해주세요.

점검/구현 요청:
- "통장 입출금" 화면에 계좌 리스트(드롭다운) UI 추가
- 계좌 선택 시 해당 계좌의 입출금 내역만 하단 표에 표시되도록 필터·렌더 로직 개선(js: accounts.js, dashboard.js 등)
- 계좌별 선택, 표 렌더, 결과 반영, 드롭다운 이벤트 처리 및 연결 함수 점검
- UI 구축 위치 및 스타일은 "카드별 내역"의 카드 선택 영역과 동일하게 구현

테스트 방법:
- "통장 입출금" 화면 진입 → 계좌 리스트(드롭다운)에서 다양한 계좌 선택하며 내역 표 데이터가 정상적으로 필터링·표시되는지 확인
- 모든 계좌별 입출금 내역이 빠짐없이 표에 반영되는지 반복 테스트
- 기존 내역, 신규 등록 후에도 계좌별 필터 동작을 재점검

히스토리:
- 신규 QA 항목(22번) 등록, 기존 21번 이어서 관리
